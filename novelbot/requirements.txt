# ---------------------------------------------------------------
# Novel Name Converter Bot  -  production requirements
# Pinned for reproducible builds on Render free tier (512 MB RAM)
# ---------------------------------------------------------------
pyrogram==2.0.106
aiohttp==3.9.5
httpx==0.27.0
python-dotenv==1.0.1

# -----------------------------------------------------------------
# TgCrypto is OPTIONAL - it is only a C acceleration for Pyrogram's
# MTProto layer. Pyrogram runs fine without it (pure-Python fallback).
# It is commented out on purpose: on some base images pip has to
# compile it from source and the build can fail (missing C toolchain),
# which would break the whole deploy. Uncomment it if your build image
# ships wheels for your Python version.
# -----------------------------------------------------------------
# TgCrypto==1.2.5

# uvloop is optional too; CPython's default loop is fine on 0.1 CPU.
# uvloop==0.19.0; python_version < "3.13" and sys_platform != "win32"
